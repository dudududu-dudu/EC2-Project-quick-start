const dlg = document.getElementById('consult-dialog');
document.getElementById('hero-btn').addEventListener('click', () => dlg.showModal());
document.getElementById('dlg-close').addEventListener('click', () => dlg.close());
